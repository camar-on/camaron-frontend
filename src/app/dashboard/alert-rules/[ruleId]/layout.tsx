export function generateStaticParams() {
  return [];
}

export const dynamicParams = true;

export default function RuleIdLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
